<!DOCTYPE html>
<html>
  <head>
    <title>SkateMate</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'main'%>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%=render 'layouts/nav'%>
    <div class="container">
      <h1 class="centered">SKATE-MATE</h1>
      <%= yield %>
    <div class="flash-alert" class="centered">
      <%flash.each do |type, msg|%>
        <%=msg%>
      <%end%>
    </div>
    </div>
    <footer class="centered">Skate-Mate 2020.</footer>
  </body>
</html>
