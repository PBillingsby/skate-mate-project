<div class="spot-view">
  <% user_spots.each do |spot| %>
    <div class="mx-auto">
      <h3><%= link_to spot.name, spot %></h3>
      <%= spot.location.city %>, <%= spot.location.country %>
    </div>
  <% end %>
</div>